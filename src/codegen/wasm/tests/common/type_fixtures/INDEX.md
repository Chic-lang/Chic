# WASM type fixture index

Generated by `cargo xtask wasm-fixtures` from `catalog.json`.

## Tables

- `tuple_layout_table`: Tuple layout table for <int, int> tuple.
- `tuple_aggregate_fixture`: MIR that aggregates a tuple value from two ints.
- `tuple_copy_fixture`: MIR that copies a tuple value between locals.
- `tuple_param_fixture`: MIR that accepts a tuple parameter and returns unit.
- `enum_layout_table`: Enum layout table for Demo::Choice and Flags::Permissions flag enum.
- `union_layout_table`: Union layout table for Demo::UnionValue with a single value view.
- `enum_projection_fixture`: MIR that carries an enum local, used for enum layout projections.
- `union_projection_fixture`: MIR that carries a union local, used for union layout projections.

## Memory

- `array_index_fixture`: Array layout plus MIR that indexes into the array by usize.
- `string_index_fixture`: MIR that indexes into a string by usize and returns a char.
- `str_index_fixture`: MIR that indexes into a str slice by usize and returns a char.
- `struct_with_missing_offsets_fixture`: Struct layout with missing field offsets for offset-diagnostic tests.
- `struct_without_size_layout`: Struct layout missing a size entry for incomplete layout handling.

## References

- `sample_pair_layout`: Struct layout for Demo::Pair used by projection and frame tests.
- `sample_class_layout`: Class layout for Demo::Window including vtable offset metadata.
- `struct_projection_fixture`: MIR that projects a struct field and returns it.

