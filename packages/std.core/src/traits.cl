namespace Std
{
    /// Marker interface for trivially copyable types.
    public interface Copy
    {
    }
    /// Marker interface for types with deterministic destruction semantics.
    public interface Drop
    {
    }
}
