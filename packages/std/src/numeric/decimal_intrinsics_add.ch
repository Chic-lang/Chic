namespace Std.Numeric.Decimal;
public static class DecimalIntrinsicsAdd
{
    public static DecimalIntrinsicResult Add(decimal lhs, decimal rhs) {
        return DecimalIntrinsics.Add(lhs, rhs);
    }
}
