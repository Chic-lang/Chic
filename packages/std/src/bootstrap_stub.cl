namespace Std.Bootstrap;
/// Minimal placeholder used by integration tests to prevent the driver from
/// eagerly loading the full bootstrap standard library while it is still
/// under active development.
public struct __BootstrapStub
{
}
