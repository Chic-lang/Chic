# Std.Compiler.Object

Foundational compiler/runtime infrastructure package that will replace the Rust “object crate”. This package owns Chic-native object metadata, glue lookup, and related runtime primitives used by the compiler and standard library.
